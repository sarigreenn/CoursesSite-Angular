import { LearningWayPipe } from './learning-way.pipe';

describe('LearningWayPipe', () => {
  it('create an instance', () => {
    const pipe = new LearningWayPipe();
    expect(pipe).toBeTruthy();
  });
});
